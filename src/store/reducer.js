import * as actionTypes from './actions';

const initState = {
    ingredients: null,
    totalPrice: 4,
};

const reducer = (state = initState, action) => {
    return state;
}

export default reducer;